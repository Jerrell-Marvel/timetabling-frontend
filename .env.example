# Base URL of the decoupled Laravel API (Axios `baseURL`).
VITE_API_URL=http://localhost:8000/api

# CSRF-cookie priming endpoint (Sanctum-style). Optional; defaults to /sanctum/csrf-cookie.
# VITE_CSRF_URL=/sanctum/csrf-cookie
