# Base URL of the Spring Boot API (Axios `baseURL`). Host is http://localhost:8085;
# keep the `/api` suffix — Spring serves every route under /api and the services
# in src/services/* call bare paths like `/courses`.
VITE_API_URL=http://localhost:8085/api

# Auth is stateless JWT — no CSRF priming endpoint is used anymore.
