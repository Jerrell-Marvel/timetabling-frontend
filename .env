# Spring Boot backend (JWT). Host is http://localhost:8085; the `/api` suffix is
# required because Spring serves every route under /api and the services in
# src/services/* call bare paths like `/courses`. Drop `/api` only if you later
# prefix those service paths instead.
VITE_API_URL=http://localhost:8085/api
